# Ski Map

Interactive local map of ski resorts and international airports.

## Run

### Windows

Double-click `start_server_windows.bat`.

The Windows launcher uses the built-in PowerShell/.NET runtime, so Python is not required.

### macOS

Double-click `start_server_mac.command`.
